# Base path for all API calls. In dev the Vite proxy forwards /api to the
# Django backend, so the default same-origin value works out of the box.
PUBLIC_API_BASE=/api/v1

# Where the Vite dev proxy forwards /api requests (Django dev server).
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
